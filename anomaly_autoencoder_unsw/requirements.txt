# Minimal deps for the UNSW-NB15 Autoencoder anomaly module
# NOTE: install a CUDA-enabled torch build on your GPU machine if needed.

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
torch>=2.0
networkx>=3.0
